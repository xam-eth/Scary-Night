# Copy to server/.env on the machine that runs the payment server.
# Never commit server/.env. The server key must not appear in the game bundle,
# in git, or in a Play Store APK.
MIDTRANS_SERVER_KEY=
MIDTRANS_MERCHANT_ID=M769336744
MIDTRANS_IS_PRODUCTION=true
PORT=8787
